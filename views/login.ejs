<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head.ejs') %> <%- include('partials/nav.ejs') %>
    <link rel="stylesheet" href="/css/auth.css" />
    <title>Login - SmartChat AI</title>
  </head>
  <body>
    <main class="auth-container">
      <section class="auth-form-side">
        <div class="auth-logo">SmartChat AI</div>
        <h2>Welcome Back</h2>
        <p>Enter your email and password to continue</p>
        <form action="/login" method="POST" class="auth-form">
          <input
            type="email"
            name="email"
            placeholder="you@company.com"
            required
          />
          <input
            type="password"
            name="password"
            placeholder="••••••••"
            required
          />
          <input
            type="tel"
            name="phone"
            placeholder="+1 234 567 8901"
            required
          />
          <button type="submit" class="btn-primary">Log In</button>
        </form>
        logg
        <div class="social-login">
          <a href="/auth/google" class="btn btn-google">
            <img src="/images/google.svg" alt="Google icon" />
            Sign in with Google
          </a>
        </div>
        <p class="switch-link">
          Don't have an account? <a href="/signup">Sign Up</a>
        </p>
      </section>
      <section class="auth-info-side">
        <h3>Effortlessly automate support & sales</h3>
        <p>
          Access your dashboard to track leads, response rates, and AI
          performance in real time.
        </p>
        <div class="info-graphic">
          <!-- You can add SVG or CSS graphic here -->
        </div>
      </section>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
